import 'package:maze_gen/core/models/grid.dart';

// Gridder is an interface for classes marking the solution for a grid.
class GridSolver {
  // mark the grid cells to solve as visited and return the number of cells
  // walked to solve the maze.
  int solve(Grid grid) {
    return 0;
  }
}
